#include <iostream>

#include "utility-functions.h"

using namespace std;

int main() {
  int x = 2, y = 3;
  int *a = &x;
  int *b = &y;

  // 1st Function: takes pointer variables as argument and return an integer
  int result1 = Add(a, b);

  cout << result1 << endl;
  return 0;
}
